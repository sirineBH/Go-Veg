'use strict';

describe('myApp.addRecipe module', function () {

  beforeEach(module('myApp.addRecipe'));

  describe('addRecipe controller', function () {

    it('should ....', inject(function ($controller) {
      //spec body
      var addRecipeCtrl = $controller('addRecipeController');
      expect(addRecipeCtrl).toBeDefined();
    }));

  });
});